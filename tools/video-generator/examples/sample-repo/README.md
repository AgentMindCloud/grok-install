# sample-repo

A tiny fixture that exercises every segment of the grok-vidgen pipeline:
analyzer (Python + TypeScript), narration (imperative + keyword hits),
Remotion project writer, and the CI bundle smoke test.

Not meant to be a real app — just small enough to render in a few seconds.
