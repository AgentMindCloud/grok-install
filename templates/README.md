## templates/

Bundled agent manifests, organized by source:

- [`community/`](community/) — community-contributed templates carried over from awesome-grok-agents.

The 8 master-plan core templates land in a follow-up PR. Each manifest validates against [`../spec/v2.14/schema.json`](../spec/v2.14/schema.json); run `grok-install validate templates/` to verify.
