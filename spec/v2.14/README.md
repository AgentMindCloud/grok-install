# spec v2.14

The canonical schema is [`schemas/bridge.schema.json`](schemas/bridge.schema.json) (JSON Schema 2020-12, per DECISIONS.md Q6). Extension blocks are documented in [`extensions/README.md`](extensions/README.md). Validate manifests via `grok-install validate <path>`.
