# spec v2.14

The canonical source of truth for the v2.14 manifest schema is the JSON Schema 2020-12 file in [`schemas/bridge.schema.json`](schemas/bridge.schema.json) per DECISIONS.md Q6. Documentation generated from the schema lives alongside it.

Optional `extensions:` blocks are documented in [`extensions/README.md`](extensions/README.md).

Status: PLACEHOLDER (Phase 2b)
