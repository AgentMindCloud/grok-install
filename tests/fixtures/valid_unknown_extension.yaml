version: "2.14"
name: future-compat
description: Exercises the forward-compat policy for unknown extension keys.
runtime:
  engine: grok
  model: grok-3
deploy:
  targets:
    - worker
extensions:
  future_block:
    placeholder: true
