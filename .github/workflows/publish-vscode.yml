name: publish-vscode

on:
  release:
    types: [published]

jobs:
  publish-vscode:
    runs-on: ubuntu-latest
    if: github.event_name == 'release'
    steps:
      - name: Placeholder
        run: echo "publish to VSCode Marketplace gated on tag, requires VSCE_PAT secret"
