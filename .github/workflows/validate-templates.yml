name: validate-templates

on:
  pull_request:
    paths:
      - 'agents/**'
      - 'cli/validate.py'

jobs:
  validate-templates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Phase 2a placeholder — agent YAML validation wires up in Phase 2b"
