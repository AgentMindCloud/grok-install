source "https://rubygems.org"

# Mirrors the gem set used by GitHub Pages' actions/jekyll-build-pages@v1
# action so local `bundle exec jekyll serve` matches what CI builds.
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-seo-tag"
end
